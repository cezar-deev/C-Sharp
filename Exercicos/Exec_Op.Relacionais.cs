
//--------------------------------Operadores relacionais-------------------------



/* Complete o código abaixo utilizando os operadores relacionais para que todas as saídas sejam igual "True":*/

using System;
public class Program
{
	public static void Main()
	{
		int a = 8, b = 40, c = 20, d = 5;
		
		Console.WriteLine( a * d == b );
		Console.WriteLine( b / d != c );
		Console.WriteLine( c == d * 4 );
		Console.WriteLine( ! (c * 2 != b) );
	    Console.WriteLine( (a / 2) * 5 == c );	   
	}
}

//--------------------------------Operadores relacionais-------------------------


